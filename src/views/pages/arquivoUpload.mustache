{{>partials/header}}

<h2>Fazer Upload de Arquivo</h2>

<fieldset>
    <form method="POST" action="/uploadnovoarquivo/{{id}}" enctype="multipart/form-data">
        <input type="file" id="arquivo" name="arquivo" multiple required>
        <p>Você pode selecionar múltiplos arquivos mantendo pressionado Ctrl ao clicar nos arquivos.</p>
        <button type="submit" class="btn-success">Enviar</button>
    </form>
</fieldset>

<div class="action-buttons">
    <a href="/cliente/{{id}}">
        <button type="button" class="btn-secondary">Cancelar</button>
    </a>
</div>

{{>partials/footer}}